<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BudgetTracker" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- Customize ActionBar style -->
        <item name="actionBarStyle">@style/DarkActionBar</item>
    </style>

    <style name="Theme.BudgetTracker" parent="Base.Theme.BudgetTracker" />

    <!-- Define a dark ActionBar style -->
    <style name="DarkActionBar" parent="Widget.AppCompat.ActionBar">
        <item name="background">@color/colorPrimaryDark</item>
        <item name="titleTextStyle">@style/DarkActionBarTitleText</item>
    </style>

    <!-- Define ActionBar title text color -->
    <style name="DarkActionBarTitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="EditText.Grey" parent="@style/EditText.Grey">
        <item name="colorControlActivated">@color/grey_60</item>
        <item name="colorControlHighlight">@color/grey_60</item>
        <item name="colorControlNormal">@color/grey_40</item>
    </style>

    <style name="myTextviewBold">
        <item name="fontFamily">@font/arial_bold</item>
    </style>

    <style name="tabTextStyle">
        <item name="fontFamily">@font/arial_medium</item>
        <item name="android:textSize">20sp</item>
    </style>

    <style name="myTextviewMedium">
        <item name="fontFamily">@font/arial_medium</item>
    </style>

    <!-- Custom dark theme for Material DatePicker -->
    <style name="ThemeOverlay.MyCustomDatePicker" parent="ThemeOverlay.MaterialComponents.MaterialCalendar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="materialCalendarTheme">@style/CustomMaterialCalendar</item>
    </style>

    <!-- Customize MaterialCalendar appearance if needed -->
    <style name="CustomMaterialCalendar" parent="Widget.MaterialComponents.MaterialCalendar">
        <!-- You can add specific color or typography customizations here -->
    </style>
</resources>
